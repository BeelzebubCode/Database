# 📘 Full Database Design Documentation
**Project:** Mental Health Consultation System (ระบบจองคิวปรึกษาสุขภาพจิต)

เอกสารฉบับนี้อธิบายโครงสร้างฐานข้อมูล (Database Schema) ทั้งหมด 30 ตาราง โดยแบ่งเป็นโมดูลตามการทำงานจริง การออกแบบยึดหลัก **Normalization** เพื่อลดความซ้ำซ้อน, รองรับ **PDPA** เพื่อความปลอดภัยของข้อมูลส่วนบุคคล, และเน้น **Performance** ในการเรียกดูข้อมูล

---

## 1. System & Authentication (ระบบผู้ใช้งานและความปลอดภัย)
ส่วนรากฐานของระบบที่จัดการเรื่องการเข้าใช้งาน การยืนยันตัวตน และ Log ความปลอดภัย

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`users`** | เก็บข้อมูล Login หลัก (Username, Password Hash, Role) | • **Centralized Auth:** รวม User ทุกประเภท (นิสิต/หมอ/Admin) ไว้ที่เดียว จัดการ Security ง่าย<br>• **Scalability:** รองรับการขยายระบบ Login ผ่าน LINE หรือ Google ในอนาคต |
| **`login_logs`** | บันทึกประวัติการเข้า-ออกระบบ (Success/Fail) | • **Security:** ตรวจจับ Brute Force Attack (ใครเดารหัสผิดบ่อยๆ)<br>• **Audit:** ตรวจสอบได้ว่า User เข้าใช้งานล่าสุดเมื่อไหร่ IP อะไร |
| **`audit_logs`** | บันทึกการ "เข้าถึงข้อมูล" (Read Access) | • **PDPA:** กฎหมายบังคับให้ตรวจสอบได้ว่า "ใครแอบส่องประวัตินิสิตคนไหน" (Read Audit)<br>• **Traceability:** ใช้สืบสวนหาคนผิดเมื่อข้อมูลรั่วไหล |

---

## 2. University Structure (โครงสร้างมหาวิทยาลัย)
ข้อมูล Master Data ขององค์กร เพื่อลดความผิดพลาดในการกรอกข้อมูล

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`faculties`** | เก็บรายชื่อคณะ | • **Consistency:** ป้องกันการพิมพ์ชื่อคณะผิด (ใช้เลือกจาก Dropdown)<br>• **Stats:** ทำรายงานสรุปยอดผู้ใช้งานแยกตามคณะได้ง่าย |
| **`departments`** | เก็บรายชื่อภาควิชา (Link กับคณะ) | • **Filtering:** กรองข้อมูลนิสิตได้ละเอียดขึ้นถึงระดับสาขาวิชา |
| **`advisors`** | เก็บรายชื่ออาจารย์ที่ปรึกษา | • **Emergency:** กรณีเคสเสี่ยงสูง (Suicide Risk) สามารถดึงเบอร์อาจารย์เพื่อประสานงานด่วนได้ |

---

## 3. Student Module (ข้อมูลนิสิต)
แยกข้อมูลส่วนตัว ข้อมูลการเรียน และที่อยู่ ออกจากกันเพื่อความยืดหยุ่นและการจัดการสิทธิ์

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`students`** | เชื่อมโยง User ID เข้ากับรหัสนิสิต | • **Logic Separation:** แยกความเป็น "ผู้ใช้ระบบ" ออกจาก "นักเรียน" (จัดการสถานะ Active/Graduated ได้ง่าย) |
| **`students_profiles`** | เก็บข้อมูลส่วนตัว (วันเกิด, ศาสนา, เพศ) | • **Privacy:** แยกข้อมูล Sensitive ออกมา เพื่อจำกัดสิทธิ์การมองเห็น (เช่น จนท.ทั่วไปไม่ควรเห็นศาสนา) |
| **`student_academic`** | เก็บข้อมูลการเรียน (เกรด, ปีที่เข้า, หลักสูตร) | • **Analytics:** วิเคราะห์ความสัมพันธ์ระหว่าง "ปัญหาสุขภาพจิต" กับ "ผลการเรียน/ชั้นปี" |
| **`student_addresses`** | เก็บที่อยู่ (ภูมิลำเนา, หอพัก) | • **Mapping:** ใช้ทำ Heatmap พื้นที่เสี่ยง หรือวิเคราะห์ปัญหาเด็กหอพัก vs เด็กบ้าน |

---

## 4. Consultant Module (ผู้ให้คำปรึกษา)
จัดการข้อมูลหมอและการจับคู่ (Matching)

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`consultants`** | ข้อมูลหลักของผู้ให้คำปรึกษา (System Data) | • **Management:** จัดการสถานะการทำงาน (Active/Inactive) และสังกัด |
| **`consultant_profiles`** | ข้อมูลทั่วไปที่โชว์หน้าเว็บ (รูป, ชื่อเล่น) | • **Display:** แยกข้อมูลสำหรับโชว์หน้าเว็บออกจากข้อมูลระบบ (System ID) |
| **`organizations`** | หน่วยงานต้นสังกัด (เช่น ศูนย์ Wellness, กองกิจฯ) | • **Grouping:** จัดหมวดหมู่หมอตามหน่วยงาน เพื่อการบริหารจัดการงบประมาณหรือรายงาน |
| **`consultant_specializations`** | ความเชี่ยวชาญ (เช่น ซึมเศร้า, ครอบครัว) | • **Search:** ช่วยให้นิสิตค้นหาหมอได้ไวขึ้นจาก Tag ปัญหา (Matching แม่นยำ) |
| **`consultant_languages`** | ภาษาที่สื่อสารได้ | • **Inclusivity:** รองรับนิสิตต่างชาติ ให้จับคู่กับหมอที่พูดภาษาเดียวกันได้ |

---

## 5. Schedule & Time (ระบบตารางเวลา)
ใช้โมเดล Inventory Management แยก "เวลาว่าง" ออกจาก "การจอง"

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`shift_defaults`** | แม่แบบตารางเวลา (Template) | • **Automation:** ลดงาน Admin ไม่ต้องสร้างตารางเองทุกวัน ใช้ Template เสกตารางรายสัปดาห์ได้เลย |
| **`time_slots`** | พื้นที่ว่างสำหรับจอง (Inventory จริง) | • **Flexibility:** ปรับเปลี่ยนจำนวนรับในแต่ละชั่วโมงได้อิสระ โดยไม่กระทบวันอื่นๆ |
| **`schedule_modifications`** | วันหยุด/วันลาพิเศษ | • **Override:** ระบบจะไม่ Gen ตารางในวันที่หมอลงบันทึกวันลาไว้ (Prevention) |

---

## 6. Booking System (ระบบการจอง)
หัวใจหลักของระบบ รองรับ Workflow ที่ซับซ้อน (จอง -> จ่ายงาน -> รักษา -> ประเมิน)

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`bookings`** | เก็บ Transaction การจองหลัก | • **State Management:** ติดตามสถานะงานปัจจุบัน (Pending -> Assigned -> Completed) |
| **`problem_categories`** | หมวดหมู่ปัญหา (Master Data) | • **Reporting:** ผู้บริหารดู Dashboard ได้ว่าเดือนนี้ปัญหาเรื่องไหนมาแรงสุด |
| **`booking_assignment`** | บันทึกการจ่ายงาน | • **Accountability:** ตรวจสอบได้ว่า "Head คนไหน" เป็นคนจ่ายงานเคสนี้ (ป้องกันการเกี่ยงงาน) |
| **`booking_logs`** | ประวัติการเปลี่ยนแปลงสถานะ | • **Timeline:** ดูย้อนหลังได้ว่าเคสนี้ถูกดองไว้นานแค่ไหน หรือถูกเปลี่ยนมือบ่อยแค่ไหน |
| **`booking_outcomes`** | บันทึกผลการปรึกษา (Medical Record) | • **Confidentiality:** แยกบันทึกทางการแพทย์ออกจากข้อมูลการจอง เพื่อความปลอดภัยชั้นสูง |
| **`booking_cancellations`** | รายละเอียดการยกเลิก | • **Data Cleaning:** แยกเหตุผลการยกเลิกออกมา เพื่อไม่ให้ตาราง `bookings` หลักรกด้วยข้อมูล Null |

---

## 7. Feedback (ระบบประเมินผล)
ออกแบบแบบ Dynamic Evaluation เพื่อรองรับการปรับเปลี่ยนเกณฑ์

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`feedbacks`** | หัวตารางการประเมิน (Header) | • **Linkage:** เชื่อมโยงว่า "ใครประเมินใคร" ใน Job ไหน (โดยสามารถเลือก Anonymous ได้) |
| **`evaluation_criteria`** | หัวข้อคำถาม (เช่น ความสุภาพ, ความรู้) | • **Scalability:** เพิ่ม/ลด คำถามได้เองผ่านหลังบ้านโดยไม่ต้องแก้โค้ด |
| **`feed_ratings`** | คะแนนดิบรายข้อ (1-5 ดาว) | • **Analysis:** คำนวณคะแนนเฉลี่ยแยกด้านได้ง่าย (เช่น หมอเก่งวิชาการ แต่คะแนนบริการต่ำ) |
| **`feedback_comments`** | ข้อเสนอแนะเพิ่มเติม (Text) | • **Performance:** แยก Text ยาวๆ ออกจากตารางคะแนน เพื่อให้ Query คำนวณเกรดได้ไวขึ้น |

---

## 8. Notifications (ระบบแจ้งเตือน)
ระบบติดตามสถานะการส่งข้อความ

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`notifications`** | Log การส่งข้อความ (Outbox) | • **Debugging:** เป็นหลักฐานว่าระบบส่งแจ้งเตือนหา User แล้วหรือยัง (ป้องกันดราม่า "ไม่เห็นเตือนเลย") |
| **`notification_templates`** | แม่แบบข้อความ | • **Maintainability:** แก้ไขข้อความแจ้งเตือนได้ที่เดียว เปลี่ยนทั้งระบบทันที |
