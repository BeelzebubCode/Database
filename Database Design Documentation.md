# 📘 Database Design Documentation
**Project:** Mental Health Consultation System (ระบบจองคิวปรึกษาสุขภาพจิต)

เอกสารฉบับนี้อธิบายโครงสร้างฐานข้อมูล (Database Schema) โดยแบ่งเป็นโมดูลตามการทำงานจริง การออกแบบยึดหลัก **Normalization** เพื่อลดความซ้ำซ้อน, รองรับ **PDPA** เพื่อความปลอดภัยของข้อมูลส่วนบุคคล, และเน้น **Performance** ในการเรียกดูข้อมูล

---

## 1. System & Authentication (ระบบผู้ใช้งานและความปลอดภัย)
ส่วนรากฐานของระบบที่จัดการเรื่องการเข้าใช้งานและความปลอดภัยสูงสุด

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`users`** | เก็บข้อมูลการเข้าสู่ระบบหลัก (Username, Password Hash, Role) | • **Centralized Security:** รวมการจัดการความปลอดภัยไว้ที่จุดเดียว ไม่ต้องแยกตาราง Login ของนิสิตกับอาจารย์ ทำให้ดูแลรักษาง่าย<br>• **Extensibility:** รองรับการขยายระบบ Login ผ่าน LINE หรือ Google ในอนาคต |
| **`login_logs`** | บันทึกประวัติการเข้า-ออกระบบ (Success/Fail) | • **Security Monitoring:** ตรวจจับพฤติกรรมผิดปกติ เช่น การพยายามเดารหัสผ่าน (Brute Force Attack)<br>• **Audit:** ตรวจสอบได้ว่าใครเข้าใช้งานระบบครั้งล่าสุดเมื่อไหร่ |
| **`audit_logs`** | บันทึกการ "เข้าถึงข้อมูล" (Read Access) เช่น การดูประวัติ, การค้นหา | • **PDPA Compliance:** หัวใจสำคัญของกฎหมายข้อมูลส่วนบุคคล ใช้ตรวจสอบว่า "ใครแอบส่องประวัตินิสิตคนไหน" (Read Audit)<br>• **Forensics:** ใช้เป็นหลักฐานดิจิทัลเมื่อเกิดข้อมูลรั่วไหล |

---

## 2. University Structure (โครงสร้างมหาวิทยาลัย)
ส่วนจัดการข้อมูล Master Data เพื่อลดความผิดพลาดในการกรอกข้อมูล

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`faculties`** | รายชื่อคณะ | • **Data Consistency:** ป้องกัน User พิมพ์ชื่อคณะผิดๆ ถูกๆ (ใช้การเลือกจาก Dropdown)<br>• **Reporting:** ใช้ทำรายงานสรุปสถิติแยกตามคณะได้ง่าย |
| **`departments`** | รายชื่อภาควิชา (สังกัดคณะ) | • **Filtering:** ช่วยให้กรองข้อมูลเจาะลึกได้ถึงระดับสาขาวิชา |
| **`advisors`** | รายชื่ออาจารย์ที่ปรึกษา | • **Emergency Contact:** กรณีเคสเสี่ยงสูง (High Risk) สามารถดึงเบอร์โทรอาจารย์ที่ดูแลนิสิตคนนั้นเพื่อประสานงานได้ทันที |

---

## 3. Student Module (ข้อมูลนิสิต)
แยกข้อมูลส่วนตัว ข้อมูลการเรียน และที่อยู่ ออกจากกันเพื่อความยืดหยุ่น

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`students`** | ตารางเชื่อม (Link) ระหว่าง User และรหัสนิสิต | • **Separation of Concerns:** แยก Logic ของ "ผู้ใช้ระบบ" ออกจาก "ความเป็นนักเรียน"<br>• **Status:** ใช้จัดการสถานะนิสิต เช่น พักการเรียน หรือ จบการศึกษา |
| **`students_profiles`** | ข้อมูลส่วนตัว (วันเกิด, ศาสนา, เพศสภาพ) | • **Privacy Control:** แยกตารางออกมาเพื่อให้จำกัดสิทธิ์การเข้าถึงได้ง่าย (เช่น เจ้าหน้าที่บางคนเห็นแค่ชื่อ แต่ไม่ควรเห็นศาสนาหรือเบอร์โทร) |
| **`student_academic`** | ข้อมูลการเรียน (เกรด, ปีที่เข้า, หลักสูตร) | • **Analytics:** ใช้ analyzing ความสัมพันธ์ระหว่าง "ความเครียด" กับ "ผลการเรียน" หรือ "ชั้นปี" |
| **`student_addresses`** | ที่อยู่ (ภูมิลำเนา, หอพัก) | • **Normalization:** รองรับกรณี 1 คนมีหลายที่อยู่ และใช้ทำ Heatmap พื้นที่เสี่ยงได้ |

---

## 4. Consultant Module (ผู้ให้คำปรึกษา)
เน้นระบบการจับคู่ (Matching) เพื่อให้นิสิตเจอหมอที่ตรงกับปัญหา

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`consultants`** | ข้อมูลหลักของผู้ให้คำปรึกษา | • **Role Management:** ระบุสังกัดและสถานะ Active/Inactive ของผู้ให้คำปรึกษา |
| **`consultant_specializations`** | ความเชี่ยวชาญ (เช่น ซึมเศร้า, การเรียน) | • **Search Optimization:** ช่วยให้ระบบค้นหาหมอได้ไวขึ้นจาก "หัวข้อปัญหา" ที่นิสิตเลือก |
| **`consultant_languages`** | ภาษาที่สื่อสารได้ | • **Inclusivity:** รองรับนิสิตต่างชาติ ให้ระบบจับคู่กับหมอที่พูดภาษาเดียวกันได้ถูกต้อง |

---

## 5. Schedule & Time (ระบบตารางเวลา)
ใช้โมเดล Inventory Management แยก "เวลาว่าง" ออกจาก "การจอง"

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`shift_defaults`** | แม่แบบตารางเวลา (Template) | • **Automation:** ช่วยให้ไม่ต้องสร้างตารางเองทุกวัน ระบบใช้ Template นี้ Gen ตารางรายสัปดาห์ให้อัตโนมัติ |
| **`time_slots`** | พื้นที่ว่างสำหรับจอง (Inventory จริง) | • **Flexibility:** สามารถปิด/เปิด หรือเพิ่มจำนวนรับในแต่ละชั่วโมงได้อย่างอิสระโดยไม่กระทบวันอื่น |
| **`schedule_modifications`** | วันหยุด/วันลาพิเศษ | • **Constraint Logic:** ป้องกันไม่ให้ระบบ Auto-gen ตารางไปทับวันที่หมอขอลางานไว้ |

---

## 6. Booking System (ระบบการจองและการรักษา)
หัวใจหลักของระบบ รองรับ Workflow ที่ซับซ้อนและการตรวจสอบย้อนหลัง

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`bookings`** | เก็บ Transaction การจองหลัก | • **Status Tracking:** ใช้ติดตามสถานะงานปัจจุบัน (Pending -> Assigned -> Completed) |
| **`booking_assignment`** | ประวัติการจ่ายงาน | • **Accountability:** ตรวจสอบได้ว่า "Head คนไหน" เป็นคนจ่ายงานเคสนี้ให้หมอ (กันการเกี่ยงงาน) |
| **`booking_logs`** | ประวัติการเปลี่ยนแปลงสถานะ (History) | • **Timeline & Debug:** ดูย้อนหลังได้ว่าเคสนี้ถูกดองไว้นานแค่ไหน หรือถูกเปลี่ยนสถานะไปมาเพราะอะไร (เก็บค่า Old vs New status) |
| **`booking_outcomes`** | ผลการปรึกษา (Medical Record) | • **Confidentiality:** แยกบันทึกทางการแพทย์ออกจากข้อมูลการจองทั่วไป เพื่อความปลอดภัยขั้นสูง |
| **`booking_cancellations`** | รายละเอียดการยกเลิก | • **Data Cleaning:** แยกข้อมูลยกเลิกออกมา เพื่อให้ตารางจองหลักสะอาด (Clean) และวิเคราะห์สาเหตุการยกเลิกได้ง่าย (Statistical Analysis) |

---

## 7. Feedback (ระบบประเมินผล)
ออกแบบให้ยืดหยุ่น รองรับการเปลี่ยนเกณฑ์ประเมินได้โดยไม่ต้องแก้โค้ด

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`evaluation_criteria`** | หัวข้อคำถาม (Dynamic Data) | • **Scalability:** Admin สามารถเพิ่ม/ลบ หัวข้อคำถามได้เองผ่านหน้าบ้าน ไม่ต้องรื้อ Database |
| **`feedback_ratings`** | คะแนนดิบรายข้อ | • **Granular Analytics:** วิเคราะห์คะแนนแยกด้านได้ละเอียด (เช่น หมอคนนี้เก่งวิชาการ แต่คะแนนความสุภาพต่ำ) |
| **`feedback_comments`** | ข้อเสนอแนะ (Text) | • **Performance:** แยก Text ยาวๆ ออกจากตารางคะแนน เพื่อให้การ Query คำนวณเกรดเฉลี่ยทำได้เร็วขึ้น |

---

## 8. Notifications (ระบบแจ้งเตือน)
ระบบติดตามสถานะการส่งข้อความ

| ตาราง (Table) | หน้าที่ (Purpose) | เหตุผลที่ต้องมี / ประโยชน์ (Key Benefits) |
| :--- | :--- | :--- |
| **`notifications`** | Log การส่งข้อความ | • **Debugging:** ใช้ยืนยันว่าระบบได้ส่งแจ้งเตือนหานิสิตแล้วจริงหรือไม่ (กรณีมีดราม่าว่าไม่ได้รับแจ้งเตือน) |
| **`notification_templates`** | แม่แบบข้อความ | • **Maintainability:** แก้ไขข้อความแจ้งเตือนได้จากจุดเดียว มีผลทั้งระบบทันที |

---

### 💡 สรุปภาพรวม (Architecture Overview)
การออกแบบนี้ช่วยแก้ปัญหาสำคัญ 3 ด้าน:
1.  **Security & Trust:** แยก Log การเข้าถึงข้อมูล (Audit) และข้อมูลการรักษา (Outcomes) ออกมาชัดเจน เพื่อรองรับ PDPA
2.  **Workflow Management:** มี `booking_logs` และ `assignments` ทำให้ติดตามงานได้ทุกขั้นตอน ไม่มีการตกหล่น
3.  **Flexibility:** การแยก `time_slots` และ `evaluation_criteria` ทำให้ระบบปรับเปลี่ยนตามนโยบายมหาลัยได้ง่ายโดยไม่ต้องแก้โปรแกรม
